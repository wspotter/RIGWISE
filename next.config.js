/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // `appDir` is now stable in Next.js; no need to set experimental flags. If you want
  // to control the App Router behavior, set `appDir: true` here (Next.js 13+ makes
  // App Router stable in later versions).
};

module.exports = nextConfig;
